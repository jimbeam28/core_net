# CoreNet 开发日志

## 2026-02-12
### 开发总结

- 按设计文档生成Packet、queue、对象池和上电模块。
- 阅读Packet代码，简化设计，去除多余的变量和方法。暂不支持buffer链。
- 阅读queue代码，将多线程简化为单线程，只考虑单线程队列。

### 下一步计划
- 阅读对象池和上电代码
- 串起单个报文创建、处理（打印到屏幕上）和释放的流程

流程设计：
（1）上电，资源初始化。
（2）编写一个测试报文，添加到收包队列
（3）处理报文。从收队列读取报文，将报文内容打印在屏幕上，然后将报文放到发包队列
（4）清空发包队列。
（5）下电，释放资源。

## 2026-02-14
### 开发总结

- 实现了上电模块、简易的报文处理模块、调度模块
- 生成“方案设计”和“代码实现”这两个skill
- 生成“代码修改”这个skill
- 打通报文处理流程
- 优化如下问题：
    - 错误类型统一性：CoreError、ProcessError、ScheduleError 三套错误类型互不转换，添加 From<T> trait 实现。
    - Scheduler dequeue 语义：dequeue() 返回 Result<Option<T>, QueueError>，Ok(None) 和 Err(Empty) 语义重叠，将其简化为 Option<T>
    - shutdown() 文档不清晰，未明确说明资源释放行为，已增强文档，明确说明 clear() 会触发 Packet 的 drop 并释放 Vec<u8> 内存

### 下一步计划
- 写一个协议分析、设计和实现的skill
- 生成ARP设计文档，实现ARP协议的处理

## 2026-02-15
### 开发总结
- 设计和实现vlan、arp
- 重构目录文件组织

### 下一步计划


## 2026-
### 开发总结
### 下一步计划