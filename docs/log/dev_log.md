# 待办清单

- [x] 梳理配置，将配置分散到各模块
- [x] 调通arp：在main中收到请求报文，生成响应报文并放到发包队列中
- [x] 重构接口、上电和vlan模块
- [x] arp模块功能不全，缺少通过接口信息回响应报文的代码
- [x] 优化目前的架构，增加测试框架
- [x] 增加报文测试框架
- [ ] 重构
- [ ] 修改协议报文的设计skill，增加实现skill

测试框架太臃肿，需要：
（1）简化测试框架。只需要创建一个接口，接收报文注入接口队列，然后调度报文处理。本模块是通用协议保温处理测试框架，和协议无关，创建报文和校验本地资源/响应报文不应该在本模块实现。

报文集成测试文件：
（1）在本文件实现创建报文和校验本地资源/响应报文。创建和校验相关的接口应该在对应的模块实现，例如和ARP报文相关的接口应该在src/protocols/arp模块实现，在本集成测试文件中只需要调接口。
（2）在所有用例执行前初始化全局资源，在所有用例执行后释放全局资源。在每个用例执行后清空全局资源。
（3）报文的测试用例序列化执行，不要并行，无需加锁

重构规则：
（1）不改变原有逻辑，只优化实现方式
（2）检查函数调用链，优化函数数量，例如将串行的短函数合并成一个
（3）检查封装性，不需要对外提供的函数改为private
（4）更新函数注释，简要说明函数功能、入参和返回值即可
（5）去除dead code和未使用变量